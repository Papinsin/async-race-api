
// export default function RacingGround() {
//   const { cars } = useCars();

//   return (
//     <section>
//       <h2>Racing Ground</h2>
//       {cars.map((car) => (
//         <div key={car.id} style={{ marginBottom: '0.5rem' }}>
//           <div>{car.name}</div>
//           <div style={{ width: `${car.speed}px`, height: '12px', background: '#ef4444' }} />
//         </div>
//       ))}
//     </section>
//   );
// }
